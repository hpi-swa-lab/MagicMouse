A chrome container around an MMBrowserMorph. Stacks an optional navigation toolbar (Back/Forward/Reload + URL field) above the browser page using a vertical TableLayout, so the toolbar never overlaps the rendered page. The frame is the SystemWindow's model, extending its title-bar menu (DevTools, reconnect, toolbar toggle). The toolbar is optional and toggleable via toolbarVisible.